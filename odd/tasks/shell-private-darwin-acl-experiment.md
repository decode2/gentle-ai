# Darwin private-file ACL experiment

## Goal and authority
At canonical parent draft PR [#4936](https://github.com/Gentleman-Programming/gentle-ai/pull/4936), head `9442ab6f`, determine on **native macOS** whether `acl_set_fd_np` with an empty extended ACL removes a permissive ACE inherited by a held, newly created `O_EXCL` empty inode, and what descriptor-bound readback reports. This is a test-only experiment, not a Darwin report writer or permission to enable one. This worktree is dedicated to the experiment. Registration RED was observed on fork dispatch; this follow-up adds only the build-tagged probe/test, the CI formatter-guard correction, and evidence. The user authorizes feature-branch work-unit commits and fork pushes for native CI/review only; the parent handles those operations. This documentation update does not authorize a main push, merge, publish, preview, or Apply.

## One bounded work unit (<=400 authored added + deleted lines)
- [ ] **Darwin inherited-ACL observation:** Add a C/Go experimental helper and native test under `darwin && cgo && privatefileaclexperiment` in `internal/privatefile/`. Use a temporary filesystem fixture only (`t.TempDir()`), never a user's Pi configuration, real report path, or report bytes. Establish a real parent `everyone` allow ACE with file inheritance, and assert the fixture actually produces a permissive inherited ACE on a new child before interpreting any result. Create the empty child with `O_CREAT|O_EXCL|O_NOFOLLOW` and owner-only requested mode, retain its fd, apply `acl_set_fd_np(fd, emptyACL, ACL_TYPE_EXTENDED)` to that fd, and read back with `acl_get_fd_np` on the **same fd**. Record pre/post ACEs and API return/errno; check `fstat` owner and mode on the held fd before/after, and path identity against the held inode. An error (including `ENOENT`), a missing inherited ACE, or unreadable/ambiguous ACL readback is **not** a successful security result. Do not use pathname ACL inspection as a substitute for same-fd readback. Treat any nonowner probe that cannot actually run as inconclusive, never as a passing confidentiality proof.
- [ ] **Strict TDD/registration:** First register a narrowly named native test in a manual `workflow_dispatch` macOS experiment lane with an exact `go test -list` presence check that fails if the build-tagged test is absent; observe registration RED before adding the test/helper. Then add the helper/test and obtain compile GREEN locally only where possible (Linux cannot compile Darwin C); run Linux checks to prove the experiment stays excluded by default, not to claim native behavior. Dispatch native macOS CI on the exact fork commit to obtain actual test output, preserving RED/observational failures rather than skipping away the precondition. Keep existing ordinary PR CI and the native unsupported-refusal test intact; the experiment lane must explicitly opt into `privatefileaclexperiment` and not become a merge gate.
- [ ] **Boundary and evidence:** Leave `internal/privatefile/report_unsupported.go` unchanged and Darwin `WriteNewReport` returning `ErrUnsupported`; add **no production writer**. Capture exact focused commands/results (registration RED, locally available compile/format checks, native test registration and run), whether an inherited ACE was proven, post-set descriptor ACL/owner/mode observations, skips and limitations. Runtime harness: native macOS temporary-filesystem fixture in the dispatch lane; Linux runs are not runtime ACL proof. Rollback boundary: remove only the build-tagged experimental helper/test and dispatch lane, leaving parent #4936 behavior untouched. If there is no safe positive native proof, keep Darwin unsupported and mark this task unfinished.

## Delivery guardrails
Forecast the helper, test, CI lane and evidence within 400 authored changed lines total; if that cannot be done without weakening assertions, stop and replan before implementation. The registration RED is an intentional interim commit before test/helper implementation; the eventual review unit keeps experiment, test and CI registration together. Feature-branch work-unit commits and fork pushes are authorized for native CI/review only; the parent handles commits and pushes. Never merge main, auto-merge, push main, or use the merge API against Gentleman-Programming without fresh exact approval. No preview, publish, or Apply without exact current approval. No AI trailers.

## Registration RED milestone (observed)
- [x] Native registration RED: fork `decode2/gentle-ai` workflow_dispatch run [36013973499](https://github.com/decode2/gentle-ai/actions/runs/36013973499), exact head `f2bca68b5727daca7fbd9bcacd26af467678c31b`, Darwin Runtime: refusal tests passed, experiment registration step exited 1 after `go test -list` exact-match pipeline; the test run line never executed. Dispatch-only CI runs `set -euo pipefail; go test -tags privatefileaclexperiment ./internal/privatefile -list '^TestDarwinInheritedACLDescriptorClearExperiment$' | grep -Fx 'TestDarwinInheritedACLDescriptorClearExperiment' >/dev/null` (as separate shell lines after `set -euo pipefail`). With no test/helper yet, the full-consuming exact-match grep should fail; the subsequent `go test -tags privatefileaclexperiment -v ./internal/privatefile -run '^TestDarwinInheritedACLDescriptorClearExperiment$' -count=1 -timeout=300s` must not run until registration succeeds. This is selector absence RED, **not** an ACL behavior result.
- Earlier Linux registration checks (disposable HOME/XDG/GOCACHE, offline module cache): default and tagged `go test ./internal/privatefile -count=1 -timeout=300s` passed; exact listing pipeline failed (Linux exclusion, not native proof). PyYAML BaseLoader parsed the initial workflow; `git diff --check` passed.
- Follow-up Linux checks with disposable `HOME`, `XDG_CONFIG_HOME`, `GOCACHE`, `TMPDIR`, `GOPROXY=off GOTOOLCHAIN=local GOMODCACHE=/home/devel/go/pkg/mod`: `go test ./internal/privatefile -count=1 -timeout=300s` PASS; `go test -tags privatefileaclexperiment ./internal/privatefile -count=1 -timeout=300s` PASS; `go test ./internal/assets -run '^TestRequiredChecksFailClosedWhenFormatFails$' -count=1 -timeout=300s` PASS; exact tagged `go test -list | grep -Fx` selector absent on Linux (expected); `go run ./internal/gofmtcheck` PASS; `git diff --check` PASS.
- Same dispatch also failed Unit Tests at `TestRequiredChecksFailClosedWhenFormatFails/darwin-runtime`: the step-level YAML `if` violated the existing required-check formatter guard (`formatter_ordering_test.go:89`). Gating now lives inside the shell via `GITHUB_EVENT_NAME`, without YAML `if`/`continue-on-error`; ordinary PR jobs still run the refusal and release-blocker steps.
- Native compile RED: fork workflow_dispatch run [36019506773](https://github.com/decode2/gentle-ai/actions/runs/36019506773) at exact head `a26b30be7a87c94cacb012df16daefdc84fe34ac` failed in tagged test listing before the experiment ran: `darwin_acl_experiment_darwin_test.go:31:15: no new variables on left side of :=`. All ten other jobs passed, including Unit Tests and the default native refusal. This is a Go test-source compile error, **not** an ACL result. The parent has already applied the separate one-line test fix (second `listing, err` reassignment uses `=`); native retry is pending. Linux cannot compile Darwin C or prove inheritance, descriptor clearing, or confidentiality. The native fixture records `ls -le` before/after plus descriptor-bound ACL text/ACE counts and owner/mode/inode identity; none of these observations is available yet. No nonowner probe is available: the test explicitly reports unavailable rather than claiming a confidentiality proof. Keep the observation checkbox unfinished; the checker remains unfinished and `internal/privatefile/report_unsupported.go` unchanged. Keep cumulative authored additions + deletions from `9442ab6f` under 400. Rollback: remove only the gated experiment step and the two build-tagged files, retain the refusal lane and parent behavior.

## Key Learnings
- Parent #4936 proves Linux private writing and native Darwin/Windows **refusal**, not Darwin ACL safety. `report_unsupported.go` currently covers every non-Linux platform.
- Cross-compilation or a skipped/nonowner-unavailable test cannot prove macOS inherited-ACL clearing; real inheritance and descriptor-bound post-set inspection on native macOS are necessary.
- Native registration RED was real selector absence, not a failing ACL test. The dispatch additionally exposed a required-check formatter ordering regression from YAML step-level gating; a shell-internal event guard preserves fail-closed ordering. Run 36019506773 failed at tagged listing due to a test-source `:=` reassignment error before ACL execution; the parent's one-line fix awaits native retry. No native ACL result exists yet.
