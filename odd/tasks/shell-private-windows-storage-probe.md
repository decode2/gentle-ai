# Windows managed-root storage probe (tagged experiment; BLOCK on ABI and identity)

**Decision:** run one bounded, strict-TDD, **test-only** native Windows capability/identity experiment before parent [managed-root unit 1b](shell-private-windows-managed-root.md). Start from isolated worktree `feat/shell-private-windows-storage-probe-04`, exact draft PR #4956 head `9cf535f02f610d1691152d8b7034c77d912a3bca`; do not start from main. Parent 1a native CI [36030750584](https://github.com/decode2/gentle-ai/actions/runs/36030750584) is green and four-lens lineage `review-070b66adf9f8259a` approved + acknowledged, but **only an untrusted held root handle** was delivered. This probe grants no trusted root, destination, writer, or physical-locality claim. It cannot unblock 1b without an independently defensible mapping-to-held-root identity contract.

## One work unit: tagged Windows capability/identity test

- [ ] **Budget / exact future edit set:** <=400 authored changed lines (additions + deletions, including CI/tests/docs) from parent `9cf535f0`. Forecast: this plan (21 lines), `.github/workflows/ci.yml` (12), future `internal/privatefile/windows_storage_probe_windows_test.go` (<=320), and later evidence edits (<=30 additions + deletions): <=383 total. Keep all helpers *in the test file* under `//go:build windows && privatefilestorageprobe`; no production storage helper. Count cumulative diff from `9cf535f0` before each delivery. If required ABI verification, identity proof, or negative coverage will exceed 400, **split/ask before code**, not by dropping refusals or enabling a partial writer. The registration-only RED is a separate reversible milestone; the later test/probe must remain one work unit.
- [ ] **CI registration RED FIRST (before helper/test write):** in the existing `windows-runtime` job, after Go setup and the formatter-success guard, add a short `shell: bash` step with `TMP` and `TEMP` set to `${{ runner.temp }}`. Branch *inside* `run:` on `GITHUB_EVENT_NAME == workflow_dispatch` (no step-level `if:`/`continue-on-error` after the formatter guard). Under `set -euo pipefail`, run `go test -tags privatefilestorageprobe ./internal/privatefile -list '^TestWindowsManagedStorageProbe$' | grep -Fx TestWindowsManagedStorageProbe >/dev/null`, then `go test -tags privatefilestorageprobe ./internal/privatefile -run '^TestWindowsManagedStorageProbe$' -count=1 -timeout=300s -v`. **Expected native RED at the exact listing:** the test does not exist yet, so `grep -Fx` exits 1 and `pipefail` fails the dispatch step before the focused test can run. Registration alone is not an observed native RED: capture exact head, fork dispatch run URL, Windows runner/OS, missing-registration failure and absence of a focused run before any helper/test is written. Ordinary PR/push CI must not execute the probe.
- [ ] **Test-first compile RED:** after the registration RED, write the tagged table-driven test and negative injection cases **before** test-local ABI/query helpers. Run an offline Windows amd64 `go test -c` with disposable `HOME`, `XDG_CONFIG_HOME`, `XDG_CACHE_HOME`, `GOCACHE`, `TMPDIR`; use explicit pre-cached `GOMODCACHE=/home/devel/go/pkg/mod`, `GOPROXY=off`, `GOTOOLCHAIN=local`, `CGO_ENABLED=0`, `GOOS=windows GOARCH=amd64`, output binary only to the disposable directory. Capture the intended undefined test-local helper compile failure, then implement only test-local bindings and capture compile GREEN. A cross-built binary cannot establish native behavior. If registration RED was not observable, do not substitute a zero-match `-run` success.
- [ ] **Fixture:** inject a `KnownFolderPath` string under `t.TempDir()` (same drive as the native fixture), never call the real known-folder provider or use the real profile. Acquire the candidate with 1a's `openWindowsManagedRootCandidate(path, queryWindowsManagedDevice, openWindowsManagedVolume)`; close the handle on every path. Keep a separate outside `t.TempDir()` sentinel and assert its exact contents after all cases. No report leaf or managed-directory creation; only disposable fixture/sentinel writes. Set native CI `HOME`, `XDG_CONFIG_HOME`, `XDG_CACHE_HOME`, `GOCACHE`, `TMP` and `TEMP` to runner-owned disposable paths (create them under `runner.temp`; verify cleanup and avoid runner profile). Keep module cache explicit/offline for local runs; record any native CI dependency/cache exceptions, not silent fallback.
- [ ] **Observed facts, not a policy gate:** on that **same held root handle**, query `GetVolumeInformationByHandle` and record filesystem name and flags; require complete, untruncated name, supported filesystem (initially NTFS only), and `FILE_PERSISTENT_ACLS`. An ACL-capable flag is not evidence of effective ACL protection. In test-local code only, resolve `NtQueryVolumeInformationFile` from `ntdll.dll` (not provided by `x/sys/windows v0.47.0`) for `FileFsDeviceInformation`; verify the API and class contract from authoritative Windows definitions before invoking. Validate pointer/ABI/layout/architecture, exact expected `FILE_FS_DEVICE_INFORMATION` size, `IO_STATUS_BLOCK` layout and `Information`, returned NTSTATUS and IO status, and device type/characteristics. Fail, never skip, on missing proc/class, unexpected status/size, unsupported device type or remote/virtual/unknown indicators. Define every flag and return expectation explicitly before coding; no guessing numeric constants or using a zeroed output on failed calls. If user-mode ntdll semantics cannot be justified, record **BLOCK** instead of treating a lucky call as a contract.
- [ ] **Identity and injected negatives:** sample `QueryDosDevice` before/after the held-handle queries; inject swaps, errors, empty/multiple or non-direct mappings, malformed ABI result/short `Information`, NTSTATUS vs IO-status disagreement, FS errors/NTFS mismatch/missing ACL bit, remote, virtual and contradictory/unknown device signals. Require a **separately measured, handle-bound root identity** and a documented public mapping-to-that-handle comparison with stable before/after observations; fail on unknown, collision-prone, mismatched or unstable identity. Do not reopen a path string as authority or infer identity merely from equal mapping strings, a matching volume serial, device-type disk, or a cleared remote flag. Sampling cannot exclude swap-away-and-back races. If no safe public identity comparison exists, the positive fixture **fails/blocks**; do not forge success with injection or downgrade to “looks local.” Under the trusted Windows OS storage-stack assumption, successful signals would still be bounded evidence, not proof of physical media or resistance to malicious kernel/drivers.
- [ ] **GREEN / evidence gate:** run the focused tagged test natively on a fork dispatch at the **exact test head** and retain URL, logs, device/FS observations (no profile paths), failures, skips (required cases must not skip), and unchanged sentinel. Run offline Windows amd64 compile + `GOOS=windows GOARCH=amd64 go vet -tags privatefilestorageprobe ./internal/privatefile`, `go run ./internal/gofmtcheck`, focused Linux `go test ./internal/privatefile ./internal/assets`, `git diff --check`, and changed-line count. Record separate registration RED, compile RED/GREEN, native negative **and** positive outcomes. Native unavailable: `N/A — native Windows runner unavailable; blocked`, not GREEN. A failed positive fixture is a useful **BLOCK finding**, not a reason to loosen it. Do not mark parent 1b complete or change `report_unsupported.go`; Windows `WriteNewReport` stays `ErrUnsupported`.

**Cost and rollback:** each fork `workflow_dispatch` triggers the whole existing CI workflow (11 jobs), not just this step. The existing Windows Runtime job has a 20-minute cap; focused test timeout is 300s. Revert the dispatch-only CI step, this tracker update and the tagged test independently of parent 1a candidate/parser/opener/unsupported writer. No push, PR, main merge, auto-merge, merge API, preview/Apply, or real config changes; no AI trailers.

**Evidence (registration → local compile; native probe pending):** at exact fork head `ac7f6c249282c4080e35ef8f5b003a4a11cfefbf`, [workflow_dispatch 36034276354](https://github.com/decode2/gentle-ai/actions/runs/36034276354) started 2026-09-24 17:26:10 UTC, completed 17:34:48 UTC (~8m38s elapsed, not billed minutes). `windows-latest` Windows Runtime runner `GitHub Actions 1000009033` failed the `go test -tags privatefilestorageprobe ... -list '^TestWindowsManagedStorageProbe$' | grep -Fx TestWindowsManagedStorageProbe` registration check (exit 1 under `pipefail`, 17:27:19 UTC); the focused run was **not executed**. The other ten jobs succeeded. After this RED, the first offline Windows/amd64 `go test -tags privatefilestorageprobe -c` failed as intended on `undefined: probeStorageFacts`, `probeDeviceResult` and subsequent test-local helpers (exit 1). After test-local implementation, offline cross-compile passed (exit 0); `GOOS=windows GOARCH=amd64 go vet -tags privatefilestorageprobe ./internal/privatefile`, Linux `go test ./internal/privatefile ./internal/assets`, `go run ./internal/gofmtcheck`, `git diff --check` passed with disposable HOME/XDG/GOCACHE/TMPDIR and `GOMODCACHE=/home/devel/go/pkg/mod GOPROXY=off GOTOOLCHAIN=local CGO_ENABLED=0` for Go invocations. A transient helper syntax error (`f.after, err :=`) was fixed before compile GREEN. No tagged native probe run exists at this unpushed test head: **N/A — native Windows run unavailable without a new authorized dispatch; BLOCK, not GREEN**. No native FS/device observations or sentinel result are claimed.

**Experiment BLOCK:** the [Microsoft NtQueryVolumeInformationFile DDI](https://learn.microsoft.com/en-us/windows-hardware/drivers/ddi/ntifs/nf-ntifs-ntqueryvolumeinformationfile) documents class/structure and notes the user-mode *name*, but its requirements specify NtosKrnl.exe/lib, not a supported ntdll.dll user-mode export/ABI contract. The test refuses to invoke an undocumented export: device status/shape/remote/virtual negatives are injected validator tests, **not** observations from a live device query. Same-handle FS flags and FileIdInfo are queried, but no documented public comparison joins a `QueryDosDevice` mapping to that held file ID; identical before/after strings can miss swap-away-and-back. The real fixture intentionally fails closed, even on NTFS. `FILE_PERSISTENT_ACLS` does not attest effective ACLs or physical media. Pending: ABI contract, identity comparison, exact-head native run + negative/positive outcomes, and runner-owned disposable CI HOME/XDG/cache setup (CI unchanged in this unit). Keep checkbox unchecked and `WriteNewReport` unsupported.

## Key Learnings

- `x/sys/windows v0.47.0` exposes `GetVolumeInformationByHandle` and `FILE_PERSISTENT_ACLS`, but not the required `NtQueryVolumeInformationFile`/`FileFsDeviceInformation` binding. The DDI's user-mode naming note is not an ntdll export guarantee: test-only injection exercises refusal while the real device query BLOCKs without invoking the DLL.
- The parent 1a held direct root is *untrusted*. Same-handle FS/device observations and two equal DOS mappings cannot alone prove which root the mapping names or prove physical locality.
- **BLOCK:** documented safe user-mode query semantics and an exact public mapping-to-held-root identity proof are missing. Preserve `ErrUnsupported` until those contracts and later parent units are independently resolved.
