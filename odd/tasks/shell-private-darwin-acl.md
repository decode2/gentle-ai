# Darwin private-report ACL probe (BLOCKED: native ACL behavior unverified)

## Goal and authority
Design and test a descriptor-bound, fail-closed Darwin ACL probe for a future owner-only private report; **do not enable a Darwin report writer**. Canonical approval: [Gentleman-Programming/gentle-ai#4935](https://github.com/Gentleman-Programming/gentle-ai/issues/4935). Base: draft [PR #4936](https://github.com/Gentleman-Programming/gentle-ai/pull/4936) head `9442ab6f`. Work on a feature-branch-chain sibling of the Windows slice; a separate **draft fork PR targeting the parent fork branch** is authorized solely for native CI. No merge, preview, Apply, or actual-user-home report writes.

Strict TDD authority: `/home/devel/.gentle-ai/state.json` (`strict_tdd: true`). Design inputs, **not runtime evidence**: [Apple ACL inheritance](https://developer.apple.com/library/archive/documentation/FileManagement/Conceptual/FileSystemProgrammingGuide/FileSystemDetails/FileSystemDetails.html) and [Apple `acl_get_fd_np`](https://developer.apple.com/library/archive/documentation/System/Conceptual/ManPages_iPhoneOS/man3/acl_get_fd_np.3.html).

## Tasks
- [ ] **Test-first probe:** Add Darwin-only focused tests that go RED before implementation, then implement a bounded ACL inspection on the *opened file descriptor* (not a pathname re-open). Reject permissive/inherited or otherwise unverifiable ACLs and every inspection/parse failure; do not treat POSIX mode bits alone as proof of privacy. Cover safe owner-only disposition and rejection cases without creating or enabling a report writer. Preserve `ErrUnsupported` for Darwin `WriteNewReport` and other unsupported platforms, with no destination creation. Keep total changed lines across the future source/tests/CI/tracker slice <=400.
- [ ] **Native evidence and review:** Keep an exact positive/negative test-presence guard in the native Darwin private-report step and run the tests on native macOS CI via the separate draft fork PR (not merely cross-compiled). The Linux host has no Darwin C cross-compiler: `GOOS=darwin CGO_ENABLED=1` RED/GREEN cross-compile checks are **not available locally** and must not be claimed. Independently verify tests, guard, format/diff and fail-closed behavior; obtain independent review and record commands, outputs, native run/commit links, test names and limits here before checking either task done. Native positive *and* negative checks are required before completion; native refusal-only evidence from parent PR is insufficient.

## Native RED and implementation candidate
- Strict native RED: fork `workflow_dispatch` run `35957742651` at `23f9755655370640c26eb41ae00a1b6fee69d4cb` failed the Darwin `go test -list` step: `internal/privatefile/darwin_acl_darwin_test.go` lines 34, 60, 82, 93 reported `undefined: probeDarwinPrivateACL`; the other 10 jobs were green. This proves compilation failed before ACL behavior could run, **not** a runtime ACL verdict.
- BLOCKED independent verification of local candidate `7210e9c1`: its `NULL`/`ENOENT` success path after `fstat` relied on an unsupported inference. Official Apple `acl_get_fd_np` documentation does not establish that `ENOENT` on a valid descriptor means no ACL (`source_check` unclear). Correction: `internal/privatefile/darwin_acl_darwin.go` still checks regular-file, owner and mode via `fstat`, then retrieves `ACL_TYPE_EXTENDED` on the same descriptor; **every NULL ACL retrieval returns an error, including `ENOENT` and missing errno**. Any returned ACL is rejected (including inherited/deny entries); entry-inspection and free failures also return errors. No path reopen, report bytes or writer change. Empty-but-present ACLs remain rejected because `acl_get_entry` cannot distinguish their end-of-list `EINVAL` from an inspection error.
- Linux offline candidate check for `7210e9c1` (before correction): with disposable writable `HOME`, `XDG_CONFIG_HOME` and `GOCACHE`, `GOPROXY=off GOSUMDB=off GOTOOLCHAIN=local GOMODCACHE=/home/devel/go/pkg/mod go test ./internal/privatefile -count=1` → `ok` (Darwin files excluded). This was not native evidence.
- Corrected-slice local checks: `mktemp -d` scratch with disposable `HOME`, `XDG_CONFIG_HOME`, `GOCACHE` and `GOPROXY=off GOSUMDB=off GOTOOLCHAIN=local GOMODCACHE=/home/devel/go/pkg/mod go test ./internal/privatefile -count=1` → `ok` (Linux only; scratch removed). `gofmt -l internal/privatefile/darwin_acl_darwin.go` → empty; `git diff --check` → clean. Darwin cgo/native ACL behavior not exercised.
- Runtime harness: native macOS behavior is **pending**. Keep `TestDarwinPrivateACLNoACLPasses` unchanged for native evidence: with fail-closed NULL handling it may now fail; do not interpret Linux checks as Darwin GREEN or mark either task done. Native positive and negative ACL behavior plus independent review are still required; do not push or open a PR from this correction.

## Limits and rollback
Runtime harness: native macOS CI for ACL behavior; no launcher/Apply route or report-write validation is authorized. If native ACL inspection cannot establish owner-only access, return failure/unsupported rather than weakening the check. Roll back only the exact probe, its tests, CI guard and this tracker; leave the parent PR and Windows sibling untouched. Tests specify no ACL accepted on an open renamed descriptor, inherited `everyone` read ACL rejected despite mode 0600, and invalid/closed/ACL-unavailable descriptors rejected. They write only empty fixtures under `t.TempDir()`; `WriteNewReport` remains unsupported on Darwin. Rollback boundary for this candidate: remove only `internal/privatefile/darwin_acl_darwin.go` and revert this tracker update; leave the earlier tests and CI guard intact. Descriptor ownership/lifetime and lack of concurrent ACL or mode mutation are caller obligations; this probe is not a writer or a proof against post-check races.
