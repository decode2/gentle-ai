# Darwin private-report ACL probe (test-only RED milestone)

## Goal and authority
Design and test a descriptor-bound, fail-closed Darwin ACL probe for a future owner-only private report; **do not enable a Darwin report writer**. Canonical approval: [Gentleman-Programming/gentle-ai#4935](https://github.com/Gentleman-Programming/gentle-ai/issues/4935). Base: draft [PR #4936](https://github.com/Gentleman-Programming/gentle-ai/pull/4936) head `9442ab6f`. Work on a feature-branch-chain sibling of the Windows slice; a separate **draft fork PR targeting the parent fork branch** is authorized solely for native CI. No merge, preview, Apply, or actual-user-home report writes.

Strict TDD authority: `/home/devel/.gentle-ai/state.json` (`strict_tdd: true`). Design inputs, **not runtime evidence**: [Apple ACL inheritance](https://developer.apple.com/library/archive/documentation/FileManagement/Conceptual/FileSystemProgrammingGuide/FileSystemDetails/FileSystemDetails.html) and [Apple `acl_get_fd_np`](https://developer.apple.com/library/archive/documentation/System/Conceptual/ManPages_iPhoneOS/man3/acl_get_fd_np.3.html).

## Tasks
- [ ] **Test-first probe:** Add Darwin-only focused tests that go RED before implementation, then implement a bounded ACL inspection on the *opened file descriptor* (not a pathname re-open). Reject permissive/inherited or otherwise unverifiable ACLs and every inspection/parse failure; do not treat POSIX mode bits alone as proof of privacy. Cover safe owner-only disposition and rejection cases without creating or enabling a report writer. Preserve `ErrUnsupported` for Darwin `WriteNewReport` and other unsupported platforms, with no destination creation. Keep total changed lines across the future source/tests/CI/tracker slice <=400.
- [ ] **Native evidence and review:** Keep an exact positive/negative test-presence guard in the native Darwin private-report step and run the tests on native macOS CI via the separate draft fork PR (not merely cross-compiled). The Linux host has no Darwin C cross-compiler: `GOOS=darwin CGO_ENABLED=1` RED/GREEN cross-compile checks are **not available locally** and must not be claimed. Independently verify tests, guard, format/diff and fail-closed behavior; obtain independent review and record commands, outputs, native run/commit links, test names and limits here before checking either task done. Native positive *and* negative checks are required before completion; native refusal-only evidence from parent PR is insufficient.

## Local test-only milestone evidence
- Linux offline: `GOPROXY=off GOSUMDB=off go test ./internal/privatefile -count=1` → `ok` (Darwin tests excluded by build tag).
- `gofmt -l internal/privatefile/darwin_acl_darwin_test.go` → empty; `git diff --check` → clean. Source readback confirms the Darwin tag, open-descriptor calls, absent production helper, and no report-writer calls. Synthetic guard listing accepts all four exact names and rejects a missing positive name; native guard execution remains pending.

## Limits and rollback
Runtime harness: native macOS CI for ACL behavior; no launcher/Apply route or report-write validation is authorized. If native ACL inspection cannot establish owner-only access, return failure/unsupported rather than weakening the check. Roll back only the exact probe, its tests, CI guard and this tracker; leave the parent PR and Windows sibling untouched. This slice adds only Darwin tests and the native CI presence guard, not a probe implementation. `probeDarwinPrivateACL(fd int) error` is deliberately undefined in production: the first native macOS CI compile should go RED on that symbol. Tests specify no ACL accepted on an open renamed descriptor, inherited `everyone` read ACL rejected despite mode 0600, and invalid/closed/ACL-unavailable descriptors rejected. They write only empty fixtures under `t.TempDir()`; `WriteNewReport` remains unsupported on Darwin. Both tasks remain pending: native CI runtime evidence, production descriptor-bound fail-closed implementation, and independent review are not yet obtained. Runtime harness: pending native macOS CI; no local native behavior claim.
